import 'package:flutter/material.dart';
//https://material.io/resources/icons/?style=baseline
Icon icon_home({double size = 18.0, Color color}) => Icon(
//  Icons.home,
  const IconData(0xe658, fontFamily: 'iconfont'),
  size: size,
  color: color,
);

Icon icon_friendcircle({double size = 18.0, Color color}) => Icon(
  const IconData(0xe613, fontFamily: 'iconfont'),
  size: size,
  color: color,
);

Icon icon_profile({double size = 18.0, Color color}) => Icon(
  const IconData(0xe611, fontFamily: 'iconfont'),
  size: size,
  color: color,
);

Icon icon_member({double size = 18.0, Color color}) => Icon(
  const IconData(0xe601, fontFamily: 'iconfont'),
  size: size,
  color: color,
);

Icon icon_puls({double size = 18.0, Color color}) => Icon(
  const IconData(0xe615, fontFamily: 'iconfont'),
  size: size,
  color: color,
);

Icon icon_prayer({double size = 18.0, Color color}) => Icon(
  const IconData(0xe60c, fontFamily: 'iconfont'),
  size: size,
  color: color,
);

Icon icon_prayer2({double size = 18.0, Color color}) => Icon(
  const IconData(0xe60d, fontFamily: 'iconfont'),
  size: size,
  color: color,
);

Icon icon_church({double size = 18.0, Color color}) => Icon(
  const IconData(0xe60e, fontFamily: 'iconfont'),
  size: size,
  color: color,
);

Icon icon_logout({double size = 18.0, Color color}) => Icon(
  const IconData(0xe631, fontFamily: 'iconfont'),
  size: size,
  color: color,
);

Icon icon_article({double size = 18.0, Color color}) => Icon(
  const IconData(0xe681, fontFamily: 'iconfont'),
  size: size,
  color: color,
);

Icon icon_author({double size = 18.0, Color color}) => Icon(
  Icons.supervised_user_circle,
  size: size,
  color: color,
);

Icon icon_left({double size = 18.0, Color color}) => Icon(
//  IconData(0xe602, fontFamily: 'iconfont'),
  Icons.chevron_left,
  size: size,
  color: color,
);

Icon icon_right({double size = 18.0, Color color}) => Icon(
//  IconData(0xe64a, fontFamily: 'iconfont'),
  Icons.chevron_right,
  size: size,
  color: color,
);

Icon icon_up({double size = 18.0, Color color}) => Icon(
  Icons.expand_less,
  size: size,
  color: color,
);

Icon icon_down({double size = 18.0, Color color}) => Icon(
  Icons.expand_more,
  size: size,
  color: color,
);

Icon icon_check({double size = 18.0, Color color}) => Icon(
//  IconData(0xe64a, fontFamily: 'iconfont'),
  Icons.check,
  size: size,
  color: color,
);

Icon icon_favorite({double size = 18.0, Color color}) => Icon(
  Icons.favorite,
  size: size,
  color: color,
);

Icon icon_favorite_border({double size = 18.0, Color color}) => Icon(
  Icons.favorite_border,
  size: size,
  color: color,
);

Icon icon_circle({double size = 18.0, Color color}) => Icon(
  Icons.radio_button_unchecked,
  size: size,
  color: color,
);

Icon icon_search({double size = 18.0, Color color}) => Icon(
  Icons.search,
  size: size,
  color: color,
);

Icon icon_setting({double size = 18.0, Color color}) => Icon(
  Icons.settings,
  size: size,
  color: color,
);

Icon icon_star({double size = 18.0, Color color}) => Icon(
  Icons.star,
  size: size,
  color: color,
);

Icon icon_star_border({double size = 18.0, Color color}) => Icon(
  Icons.star_border,
  size: size,
  color: color,
);

Icon icon_feedback({double size = 18.0, Color color}) => Icon(
  Icons.feedback,
  size: size,
  color: color,
);

Icon icon_comment({double size = 18.0, Color color}) => Icon(
  Icons.comment,
  size: size,
  color: color,
);

Icon icon_more_horiz({double size = 18.0, Color color}) => Icon(
  Icons.more_horiz,
  size: size,
  color: color,
);

Icon icon_more_vert({double size = 18.0, Color color}) => Icon(
  Icons.more_vert,
  size: size,
  color: color,
);

Icon icon_report({double size = 18.0, Color color}) => Icon(
  Icons.report,
  size: size,
  color: color,
);

Icon icon_content_copy({double size = 18.0, Color color}) => Icon(
  Icons.content_copy,
  size: size,
  color: color,
);

Icon icon_close({double size = 18.0, Color color}) => Icon(
  Icons.close,
  size: size,
  color: color,
);

Icon icon_send({double size = 18.0, Color color}) => Icon(
  Icons.send,
  size: size,
  color: color,
);

Icon icon_query_builder({double size = 18.0, Color color}) => Icon(
  Icons.query_builder,
  size: size,
  color: color,
);
Icon icon_turned_in({double size = 18.0, Color color}) => Icon(
  Icons.turned_in,
  size: size,
  color: color,
);
Icon icon_turned_in_not({double size = 18.0, Color color}) => Icon(
  Icons.turned_in_not,
  size: size,
  color: color,
);
Icon icon_text_rotate_up({double size = 18.0, Color color}) => Icon(
  Icons.text_rotate_up,
  size: size,
  color: color,
);
Icon icon_pending({double size = 18.0, Color color}) => Icon(
  Icons.pending,
  size: size,
  color: color,
);
Icon icon_wifi_off({double size = 18.0, Color color}) => Icon(
  Icons.wifi_off,
  size: size,
  color: color,
);
Icon icon_info({double size = 18.0, Color color}) => Icon(
  Icons.info,
  size: size,
  color: color,
);
Icon icon_email({double size = 18.0, Color color}) => Icon(
  Icons.email,
  size: size,
  color: color,
);
Icon icon_phone({double size = 18.0, Color color}) => Icon(
  Icons.phone,
  size: size,
  color: color,
);